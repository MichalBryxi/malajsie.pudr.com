import makeActive from '../../../utils/make-active';
import { module, test } from 'qunit';

module('Unit | Utility | make active');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = makeActive();
  assert.ok(result);
});
